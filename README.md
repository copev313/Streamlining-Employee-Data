# Streamlining-Employee-Data
 We import some employee and human resources data in CSV, Excel, and JSON format. Then merge it all into a single DataFrame, before cleaning it and exporting our results, turning it into a single CSV file.
